<% content_for :mainpage do %>
  <h1>This is Diagnoses index</h1>

  <div class="container mt-4">
    <div class="row">
      <% @diagnoses.each do |diagnosis| %>
        <div class="col-md-4 col-sm-6 mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= diagnosis.name %></h5>
              <p class="card-text"><%= diagnosis.description %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
